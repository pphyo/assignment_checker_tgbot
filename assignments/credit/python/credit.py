class Credit:
    def validate_credit_card(self, card_number: str) -> str:
        """
        Validates a credit card number using Luhn's algorithm and checks its type.
        Returns "AMEX", "MASTERCARD", "VISA", or "INVALID".
        """
        # TODO: Write your implementation here
        return "INVALID"